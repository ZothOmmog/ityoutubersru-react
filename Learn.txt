Ссылка на видео: https://youtu.be/7v29VYuPrgM

Заметки:
1. Функциональные компоненты имеют тип React.FC
2. Импортировать из index.tsx
3. Все размеры шрифтов указывать сразу в переменных
	3.1 Оборачивать размеры шрифтов в rem'ы (для адаптива)
	3.2 Цвета шрифтов тоже указывать в переменных
4. Создать базовую переменную $base для отступов, размеров и т.д. 
	(чтобы, например все отступы делились на неё и задавать их через умножение.)
5. Вынести в переменные продолжительность анимации (transition-duration)
6. Если белый цвет, то тоже имеет смысл вынести в переменную
7. Иконку ютуба экспортирует как svg и вставляет прямо в tsx
8. overflow: hidden; text-overflow: ellipsis; - для троеточия, если текст слишком длинный

Изучить:
1. Рефакторинг переменных в vscode
2. *, *::before, *::after { box-sizing: border-box }
3. models.ts - файл в котором лежат модели интерфейсов ~1:21:00
4. enums.ts - файл в котором лежат enum'ы (что это???) ~1:25:50
5. interface IProps, React.FC<IProps> 1:30:16
6. React.ReactNode[] - тип данных для массива элементов реакт 1:43:43
7. useState<ITag[]>([]) - что за конструкция??
8. npm install @types/classnames